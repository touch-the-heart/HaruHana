import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  dotStyle: {
    backgroundColor: '#D9D9D9',
    // bottom: 0,
    // top: 50,
    // position: 'relative',
    // top: 50,
    // marginTop: 20,
    // marginBottom: 400,
  },
});
